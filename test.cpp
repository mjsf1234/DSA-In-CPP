#include <iostream>
using namespace std;
#include <vector>

// todo: find the maximum
int maxProfit(int a[], int size)
{
    int buy = INT32_MAX;
    int sell = INT32_MIN;
    int ans = 0;
    for (int i = 0; i < size; i++)
    {
        buy = min(buy, a[i]);
        sell = max()
    }
}
int main()
{

    return 0;
}